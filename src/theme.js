// src/theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#2a2a72', // 主色调
        },
        secondary: {
            main: '#99ccff', // 次色调
        },
    },
});

export default theme;